apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: postgresql
    targetRevision: 18.0.8
    helm:
      values: |
        image:
          repository: bitnamilegacy/postgresql
          tag: "17.6.0"
          pullPolicy: IfNotPresent
        auth:
          postgresPassword: ZjPLOUmLMv
        primary:
          service:
            type: ClusterIP
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
